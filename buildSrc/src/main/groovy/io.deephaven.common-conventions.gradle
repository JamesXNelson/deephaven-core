plugins {
    id 'base'
    id 'io.deephaven.lifecycle-conventions'
}

group = 'io.deephaven'
String v = findProperty('version')
version = v == 'unspecified' ? '0.16.6' : v

if (!name.startsWith('deephaven-')) {
    archivesBaseName = "deephaven-${name}"
}
